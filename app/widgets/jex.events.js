(function (jex) {
    jex.events = {
        initContextMenu: "initContextMenu",
        updateBadgeText: "updateBadgeText",
        addJobToQueue: "addJobToQueue",
        notify: "notify",
        updateJob: "updateJob"
    }
})(this.jex = this.jex || {});
